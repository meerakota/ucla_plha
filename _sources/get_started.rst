

Get started
=======================

Installation
------------

The **ucla_plha** package can be installed through the Python Package Index (PyPI) using the command below.

::

    pip install ucla_plha


Example Usage
=============

After installing the package, it can be imported and used as shown below.

::

    import ucla_plha
    
    output = ucla_plha.get_hazard("config.json")
